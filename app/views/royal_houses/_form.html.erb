<%= form_for(@royal_house) do |f| %>
  <% if @royal_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@royal_house.errors.count, "error") %> prohibited this royal_house from being saved:</h2>

      <ul>
      <% @royal_house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <fieldset>
		<legend>Alliance</legend>
		<% RoyalHouse::ALLIANCES.each do |x| %>
			<%= f.radio_button :alliance, x %><%= x %><br>
		<% end %>
	</fieldset>	
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
